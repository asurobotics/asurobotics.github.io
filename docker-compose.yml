version: '2.1'

services:
  jekyll:
    container_name: jekyll
    image: jekyll/builder
    command: bash -c "sleep infinity"
    volumes:
      - ./:/srv/jekyll
    ports:
      -  4000:4000
#      - 35729:35729
#      -  3000:3000
#      -    80:4000
